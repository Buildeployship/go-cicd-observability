services:
  relay:
    build: .
    container_name: webhook-relay
    ports:
      - "8080:8080"
    environment:
      - OTEL_COLLECTOR_ENDPOINT=host.docker.internal:4318
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
